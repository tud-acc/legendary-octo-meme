html
    head
        title GeoInformatik Geländespiel
        link(rel='stylesheet', href='css/style.css')


    body(onload="mapSetup()")
        div(class='header')
            div(class='headerbox')
                include header.pug

        h2 GeoGame Emulator 
        | Input-Mode : 
        span#inputmode
        
        div#map.map(style="height:800; width: 1200;")
        
        button(type='button' class='button blue' onclick='switchInputMode(0)') Update Position  
        button(type='button' id='btn_bomb' class='button blue' onclick='switchInputMode(3)' disabled) Setze Bombe
        button(type='button' id='btn_scan' class='button blue' onclick='useScan()' disabled) Nutze Scan
        
        div#info
            h4 Debug Info:
            
            | Letzter klick:
            span#lastPos
